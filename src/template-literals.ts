{
  const multipleLineString = `
  hi how are you
  I'm fine thank you
  Bye bye
`;
  const name = 'eyal';
  const embedded = `my name is ${name}`;

  const classes = `header ${isLargeScreen() ? 'yes' : 'no'}`;
}

function isLargeScreen() {
  return true;
}
